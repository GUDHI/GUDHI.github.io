stages:
  - test
  - deploy

run_tests:
  stage: test
  
  script:
  - jekyll build

pages:
  stage: deploy
  script:
  - jekyll build
  - mv _site public
  artifacts:
    paths:
    - public
  only:
  - master
